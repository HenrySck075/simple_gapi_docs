site_name: Simple Google API References
theme:
  name: material
  features:
    - navigation.instant  # SPA-like feel (very fast)
    - navigation.tracking # URL updates as you scroll
    - navigation.expand   # Keeps the sidebar manageable
    - navigation.tabs     # High-level grouping for your 250 services
    - search.highlight
    - search.share
